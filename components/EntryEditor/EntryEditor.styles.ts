import { css } from 'emotion';

export const styles = {
	container: css({
    maxWidth: '820px',
    margin: '0 auto'
	}),
  title: css({
    fontSize: '1.65rem'
  }),
  reference: css({
    position: 'absolute',
    bottom: '8px',
    right: '20px',
  })
};